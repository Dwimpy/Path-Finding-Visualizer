// use crate::view::window::Window;

use std::rc::Rc;
use sdl2::pixels::Color;
use sdl2::rect::Rect;
use sdl2::render::{Canvas, WindowCanvas};

pub struct Renderer{

}

impl Renderer{
    pub fn new () -> Self {
        Renderer {  }
    }

}